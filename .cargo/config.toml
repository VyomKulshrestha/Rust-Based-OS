# HelioxOS Cargo Build Configuration
# Configures cross-compilation for bare-metal x86_64 target

[build]
target = "x86_64-unknown-none"

[target.x86_64-unknown-none]
runner = "bootimage runner"
rustflags = ["-C", "relocation-model=static", "-C", "link-arg=-no-pie"]
